# TODO

- [ ] Lancer `npm run build` pour générer le dossier `dist/` destiné à l’hébergement
- [x] Vérifier que `dist/` existe et contient `dist/client` + assets
- [x] Exécuter `npm run preview` pour valider le rendu local



